package com.kirkkt.java.tax.forms;

import com.kirkkt.java.tax.Parser;

import java.util.List;

public class IntListEntry {
// public class IntListEntry extends Entry<List<IntListEntryItem>> {

  // @Override
  // public void readFromLine(String line, String prefix) throws IllegalArgumentException, NumberFormatException {
  //   super.readFromLine(line, prefix);
  //   String[] toParse = (line.split(prefix, 2)[1]).split(" ");
  //   ImmutableList.Builder<IntListEntryItem> builder = ImmutableList.<IntListEntryItem>builder();
  //   for (int i = 0; i < toParse.lenth; i += 2) {
  //     builder.add(new IntListEntryItem(toParse[i], Parser.parseAndRound(toParse[i + 1], line)));
  //   }
  //   setValue(builder.build());
  // }

  // @Override
  // public String forcePrint() {
  //   return getId() + ":\n" + Printer.print(getValue()) + "\n";
  // }
}
